#include <bits/stdc++.h>
using namespace std;

vector<int> arr;

void precomp() {
    
}

int main() {
    int n; scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        int x; scanf("%d", &x);
        arr.push_back(x);
    }
    int m; scanf("%d", &m);
    for (int i = 0; i < m; i++) {
        ll x; scanf("%lld", &x);
    }
}